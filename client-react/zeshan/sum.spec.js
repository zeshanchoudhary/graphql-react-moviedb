const { sum } = require('./helper');

test('adds 1 + 2 to equal 3', () => {
  expect(sum(1, 2)).toEqual(3);
});

test('adds 1 + 2 to equal 3', () => {
  expect(sum(1, 2)).toEqual(3);
});
describe('Sum', () => {

  it.only('adds 1 + 2 to equal 3', () => {
    expect(sum(1, 2)).toEqual(3);
  });
  test('adds 1 + 2 to equal 3', () => {
    expect(sum(1, 2)).toEqual(3);
  });
  test('adds 1 + 2 to equal 3', () => {
    expect(sum(1, 2)).toEqual(3);
  });
})
